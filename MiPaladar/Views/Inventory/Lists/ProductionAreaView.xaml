<Window x:Class="MiPaladar.Views.ProductionAreaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        Title="{Binding DisplayName}" 
        
        Height="150" Width="300">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/resources/MergedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nombre:"/>

        <Button Content="Eliminar" Command="{Binding RemoveCommand}"
                ToolTip="Eliminar esta área" HorizontalAlignment="Right"/>

        <TextBox Grid.Row="1" Text="{Binding Name,UpdateSourceTrigger=PropertyChanged}"/>

        <DockPanel Grid.Row="3" HorizontalAlignment="Right"
                   Visibility="{Binding HasPendingChanges,Converter={StaticResource btvConverter}}">

            <Button Content="Guardar" Command="{Binding SaveCommand}"/>

            <Button Content="Cancelar" Command="{Binding CancelCommand}"/>

        </DockPanel>

    </Grid>

</Window>
