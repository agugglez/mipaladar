<UserControl x:Class="MiPaladar.Views.TotalesPorMesa"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <!--Mesas-->
        <DataGrid Name="dgTotalesMesas" IsReadOnly="True" AutoGenerateColumns="False"
                  GridLinesVisibility="None"
                  ItemsSource="{Binding TotalsMesa}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mesa" Binding="{Binding Path=Mesa}"/>
                <DataGridTextColumn Header="Clientes" Binding="{Binding Path=TotalClientes}"/>
                <DataGridTextColumn Header="Ventas" Binding="{Binding Path=TotalVentas,StringFormat=c}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
