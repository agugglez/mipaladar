<UserControl x:Class="MiPaladar.Views.TotalesPorCentro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <!--Centros-->
        <DataGrid Name="dgTotalesCentros" IsReadOnly="True" AutoGenerateColumns="False"
                  GridLinesVisibility="None" 
                  ItemsSource="{Binding TotalsCentro}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Path=Centro.Name}"/>
                <DataGridTextColumn Header="Clientes" Binding="{Binding Path=TotalClientes}"/>
                <DataGridTextColumn Header="Mesas" Binding="{Binding Path=TotalMesas}"/>
                <DataGridTextColumn Header="Ventas" Binding="{Binding Path=TotalVentas,StringFormat=c}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
